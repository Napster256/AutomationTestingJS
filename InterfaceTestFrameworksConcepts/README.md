# Архитектура E2E-фреймворков тестирования Web
## Принцип работы, потоки команд и различия подходов

Документ объясняет **как именно работают современные тестовые фреймворки**,
на уровне архитектуры, протоколов и исполнения команд в браузере.

Основан на визуальных схемах из папки `InterfaceTestFrameworksConcepts`.

---

## 🎯 Зачем это понимать тестировщику

Понимание архитектуры позволяет:
- писать **стабильные тесты**
- понимать, **где именно исполняется код**
- осознанно выбирать фреймворк
- правильно работать с ожиданиями, прокруткой, асинхронностью
- быстрее отлаживать падения тестов

---

## 🧱 Общая проблема, которую решают фреймворки

JavaScript / Python код теста:
- **не умеет напрямую управлять браузером**
- браузер **изолирован** от Node.js / Python
- нужен **протокол + посредник**, который переведёт команды

---

## 1️⃣ Архитектура Selenium / WebDriver

### 🔗 Компоненты схемы
```
Test Code (Python / JS) - он преобразует твой код в стандартные Selenium-команды (например, «открой страницу», «кликни кнопку»).
↓
WebDriver API - это как «переводчик» между твоим кодом и реальным браузером.
Он отправляет команды от твоего теста в браузер и возвращает ответы (создаёт HTTP-запрос)
↓
Browser Driver (chromedriver, geckodriver) - получает команды от WebDriver и выполняет их в реальном браузере (щелчок мыши, ввод текста, навигация и т.п.).
↓
Browser (Chrome, Firefox) - Это уже настоящий браузер, который физически исполняет действие
```
### 🔄 Как проходит одна команда

1. фрагмент кода:
   ```python
   driver.find_element(By.ID, "login").click()
   ```
2. WebDriver API:

- сериализует команду в HTTP-запрос

3. Browser Driver:

- переводит команду в внутренний язык браузера

4. Браузер:

- физически исполняет действие

- возвращает результат обратно

### 📌 Ключевые особенности

- стандарт W3C

- кроссбраузерность

- браузер ничего не знает о тестах

- ожидания чаще явные

- медленнее, но максимально универсально

## 2️⃣ Архитектура Chrome DevTools Protocol (CDP)

### 🔗 Компоненты
```
Test Code (Node.js) - Это то место, где запускается тест
        ↓
Chromium инстанс (создаёт JSON-команду)
        ↓
Chrome DevTools Protocol - (через постоянное соединение физический Browser получат JSON-команду)
        ↓
Chrome Browser - физически выполняет действие и возвращает результат
```
### 🔄 Принцип работы

- CDP — родной протокол Chrome

- команды идут напрямую в браузер

- без WebDriver

- без HTTP

- через WebSocket

### 📌 Особенности

- высокая скорость

- доступ к внутренностям браузера

- только Chromium

- не является стандартом W3C
- Playwright работает по идентичному принципу

## 3️⃣ Puppeteer — «инстанс Chrome»
### ❓ Почему его так называют

Puppeteer:

- запускает браузер сам

- управляет им через CDP

- полностью контролирует жизненный цикл браузера

```
Puppeteer = Chrome + CDP + Node.js API
```

📌 Браузер здесь — часть тестовой системы, а не внешний объект.

## 4️⃣ Playwright — гибридная архитектура

### 🔗 Компоненты
```
Test Code (Node.js)
        ↓
Playwright API
        ↓
Browser Server
        ↓
CDP / WebDriver-like layer
        ↓
Browser (Chromium / Firefox / WebKit)
```
### 🔄 Принцип работы

- Chromium → CDP

- Firefox / WebKit → собственные протоколы

- Playwright абстрагирует различия (скрывает от разработчика сложности взаимодействия с разными браузерами (Chromium, Firefox, WebKit) и операционными системами, предоставляя единый, простой API для написания тестов, которые работают одинаково в любой среде, обеспечивая кросс-браузерную совместимость и надежность)

- тесты выглядят одинаково для всех браузеров

### 📌 Почему его выбирают

- auto-wait из коробки

- async/await

- изоляция браузерных контекстов

- высокая стабильность

- современная архитектура

## 5️⃣ Cypress — выполнение ВНУТРИ браузера

Selenium WebDriver — это универсальный протокол для управления браузером.
Он создавался как универсальный мост между любым языком программирования и браузером.

Protractor — это надстройка над WebDriver, созданная специально для тестирования Angular-приложений.

### 🔗 Архитектура

```
Test Code
   ↓
Cypress Runner
   ↓
Injected JS (в страницу)
   ↓
Browser JS Engine
```

### 🔄 Как проходит команда

1. Команда добавляется в очередь

2. Cypress:

- ждёт DOM

- ждёт сетевые события

3. Команда исполняется в том же JS-контексте, что и сайт

① Node.js Process

 - Здесь крутится “серверная часть” Cypress (ты запускаешь npx cypress open).

 - Она:

 - управляет запуском браузера,

 - проксирует все HTTP-запросы,

 - собирает логи, делает скриншоты, видео и т.д.

② Proxy

 - Cypress создаёт локальный прокси-сервер между браузером и сайтом.

 - Все запросы (GET, POST) проходят через него.

- Прокси может перехватывать, изменять, замедлять запросы (для тестов).

③ Browser (самое важное)

 - Cypress вставляет два фрейма (iframe):

 - `Application iframe` — это реальное приложение, твой сайт.

 - `Test iframe` — это окружение Cypress, где выполняются тесты.

- Они общаются между собой через WebSocket.

### 📌 Последствия архитектуры

- ✔ автоматические ожидания
- ✔ отличная отладка
- ✔ простота написания
- ✖ ограничения по вкладкам
- ✖ нестандартная модель async

## 6️⃣ TestCafe — подмена и прокси

### 🔗 Принцип
```
Test Code
   ↓
TestCafe Server
   ↓
Proxy
   ↓
Modified Page (with injected scripts)
   ↓
Browser
```
Левая часть — это нормальная жизнь браузера без автоматизации.

1. Пользователь открывает страницу (например, `https://example.com`).

2. Браузер посылает HTTP-запрос на сервер.

3. Сервер отвечает HTML, CSS, JS.

4. Браузер отрисовывает страницу.

5. Всё взаимодействие идёт напрямую между браузером и сервером.

📌 То есть тут нет никакого посредника — браузер напрямую получает оригинальный сайт.

Правая часть - это механизм работы TestCafe:

- тест Node.js локально
- Прокси TestCafe поднимает свой прокси-сервер (см. справа «URL Rewriting Proxy»). Он сидит между браузером и настоящим сайтом.
- Эмуляция работы браузера Браузер не знает, что с ним работает TestCafe.
Для него сайт выглядит обычным, только теперь он слегка «изменён» —
HTML немного дополнен служебным JS.
- Эмуляция запросов и ответов:
1. TestCafe копирует оригинальные запросы браузера;

2. отправляет их на настоящий сайт;

3. получает ответ (HTML, JS, CSS);

4. модифицирует этот ответ;

5. возвращает в браузер уже изменённый HTML, где добавлены скрипты управления.
- Выполнение тестов в реальном браузере

Теперь TestCafe управляет браузером через свои скрипты,
а не через драйвер или DevTools Protocol.

это работает так:

- TestCafe (Node.js) → отправляет команду через прокси;

- скрипт внутри браузера получает команду;

- находит элемент a и вызывает клик;

- результат возвращается обратно TestCafe;

- TestCafe продолжает тест.
### ❓ Зачем подмена, а не оригинал

1. невозможно внедриться в оригинальный сайт

2. TestCafe:

- перехватывает HTML

- внедряет свои агенты

- управляет DOM и событиями

📌 Это не копия сайта, а модифицированный поток.

## 7️⃣ Почему не существует «одного переводчика для всех»

Причины:

- браузеры развивались независимо

- CDP ≠ WebDriver

- разные движки (Blink, Gecko, WebKit)

- WebDriver — стандарт, но медленный

- CDP — быстрый, но нестандартный

## 8️⃣ Где реально исполняется код
```
Тестовый код = Node.js / Python
WebDriver = Driver-процесс
CDP = Browser Engine
Cypress = Внутри страницы
TestCafe = Внедрённые агенты
```
## 9️⃣ Главное архитектурное различие
```
Selenium = ВНЕ браузера
Playwright = Между тестом и браузером
Cypress = ВНУТРИ браузера
Puppeteer = Браузер => часть теста
TestCafe = Через прокси и подмену
```

Архитектура фреймворка напрямую определяет
стабильность тестов, стиль кода и возможности.

## CodeceptJS

CodeceptJS — это высокоуровневый фреймворк («обёртка»), который даёт удобный синтаксис (I — «интерфейс» действий).
Под капотом он не сам управляет браузером, а делегирует действия helpers — конкретным драйверам/интеграциям (WebDriverIO, Puppeteer, Nightmare, Protractor и т.д.). Эти helpers уже общаются с браузером через соответствующие протоколы (WebDriver API, DevTools/C﻿DP, Electron API).

1. CodeceptJS (верхний блок) Точка входа для теста, набор команд

`I` — интерфейс с наборами команд (click, fillField, see и т.д.). Эти команды не выполняют действия напрямую, а вызывают методы выбранного helper.

2. Helpers (строка под CodeceptJS)

 - WebDriverIO, Protractor, Nightmare, Puppeteer и т.п. — это плагины/модули, которые реализуют конкретные действия.

 - Выбираешь helper в codecept.conf.js. Пример:

 - Puppeteer → использует CDP (DevTools Protocol) и напрямую управляет Chromium.

 - WebDriver/WebDriverIO → использует W3C WebDriver (через chromedriver/geckodriver или Selenium Server).

 - Nightmare / Electron → через Electron API (headful Electron-браузер).
3. Технологический уровень (ниже helpers)

 - DevTools Protocol — браузерный интерфейс Chromium, по которому Puppeteer/Playwright шлют команды напрямую (через WebSocket).

 - WebDriver API — HTTP-основанный протокол (W3C WebDriver). Helpers (WebDriverIO/Protractor) формируют HTTP-запросы к драйверу (chromedriver/geckodriver) или к Selenium Server.

 - Electron — для приложений на Electron (Nightmare/и др.).

4. Selenium Server / Browser Drivers

 - Когда используется WebDriver-подход, между тестом и браузером есть chromedriver/geckodriver или Selenium Server (Grid).

 - Selenium Server может распределять тесты по «узлам» и подключаться к облачным браузерам (Cloud Browsers).

5. Реальные браузеры (нижний уровень)

 - Конечные исполнители: Chrome, Firefox, Edge, Safari или headless/embedded браузеры (Electron).

 - В зависимости от helper’а команды доходят:
 - Puppeteer → CDP → Chrome

 - WebDriverIO → WebDriver → chromedriver → Chrome

 - WebDriverIO → Selenium Server → Node → Chrome/Firefox/Edge

 - Nightmare → Electron → встроенный Chromium

## Поток одной команды (пошагово)

1. пишешь `I.click('Login')` в тесте.

2. CodeceptJS передаёт запрос выбранному helper.

3. Helper переводит вызов в низкоуровневую команду:

 - Puppeteer: `Runtime.evaluate` / `Input.dispatchMouseEvent` (CDP)

 - WebDriverIO: `POST /session/:id/element/:id/click` (WebDriver)

4. Команда приходит в браузер (через CDP или через драйвер), браузер выполняет клик.

5. Результат/ошибка возвращаются обратно через цепочку helper → CodeceptJS → твой тест.

### Как выбрать helper (коротко)

Puppeteer — если нужен быстрый и глубокий контроль Chromium (локально), CDP-функции (network, tracing).

Playwright (через соответствующий helper) — если нужен мультибраузерный, мощный функционал.

WebDriver / WebDriverIO — если важна кроссбраузерность и совместимость с Grid/облаками.

Nightmare / Electron — для тестирования Electron-приложений.

## Пирамида тестирования

```
UNIT => Jest, Pest 4, Pytest (js , php , python)
INTEGRATION => Jest (с Supertest / Axios), Pest 4 (Feature tests), Pytest + Requests / aiohttp (js , php , python), postman
E2E => Playwright, Selenium + Pytest, Cypress (js , python)
PERFORMANCE => k6 / Locust / JMeter (js , python) = нагрузка
db инструменты => на скрине
Дополнительно к PERFORMANCE :
OWASP ZAP / Burp = Безопасность
```

## Документация

1. [Selenium](https://www.selenium.dev/)
2. [Playwright](https://playwright.dev/)
3. [CodeceptJS](https://codecept.io/)
4. [Cypress](https://www.cypress.io/#create)
5. [TestCafe](https://testcafe.io/)
6. [Puppeteer](https://pptr.dev/)
7. [Jest](https://jestjs.io/)
8. [Mocha](https://mochajs.org/)
9. [Chai](https://www.chaijs.com/)